name: Go Test

on:
  push:
    branches:
      - main  # Trigger on push to main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    #   - name: Checkout code
    #     uses: actions/checkout@v3

    #   - name: Set up Go
    #     uses: actions/setup-go@v5
    #     with:
    #       go-version: '1.21' # or the version you use

      - name: Run Go tests
        run: go test ./... -v
